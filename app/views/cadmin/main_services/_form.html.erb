<%= form_with(model: main_service) do |form| %>
  <% if main_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(main_service.errors.count, "error") %> prohibited this main_service from being saved:</h2>

      <ul>
        <% main_service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

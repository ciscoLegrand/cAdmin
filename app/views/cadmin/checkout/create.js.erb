var stripe = Stripe("<%= ENV['STRIPE_PUBLIC_KEY'] %>"); // TODO: finalize this section

stripe.redirectToCheckout({
  sessionId: '<%= @session.id %>'
}).then(function (result) {
  console.log(result.error.message);
}); 

